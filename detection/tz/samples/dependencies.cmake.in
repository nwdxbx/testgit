INCLUDE(GetPrerequisites)
GET_PREREQUISITES(@ANALYSIS_DEMO_LOCATION@ DEPENDENCIES 0 0 "" "")

FOREACH(libfile ${DEPENDENCIES})
#   GET_FILENAME_COMPONENT(DEPENDENCY_NAME "${DEPENDENCY}" NAME)
#   GET_FILENAME_COMPONENT(DEPENDENCY_ACTUAL "${DEPENDENCY}" REALPATH)
#   set(DEPENDENCY_NAME ${DEPENDENCY})
#   gp_resolve_item("@MY_BINARY_LOCATION@" "${DEPENDENCY}" "" "" DEPENDENCY_ACTUAL)
   
#    FILE(INSTALL
#      DESTINATION "${CMAKE_INSTALL_PREFIX}/3rd"
#      TYPE EXECUTABLE
#      RENAME "${DEPENDENCY_NAME}"
#      FILES "${DEPENDENCY_ACTUAL}"
#      )
     #message(libfile)
     file(INSTALL ${libfile} DESTINATION $ENV{DESTDIR}${CMAKE_INSTALL_PREFIX}/lib)
     if (IS_SYMLINK ${libfile}) 
         get_filename_component(libfile_target ${libfile} REALPATH) 
         file(INSTALL ${libfile_target} DESTINATION $ENV{DESTDIR}${CMAKE_INSTALL_PREFIX}/lib)
     endif()
ENDFOREACH()
